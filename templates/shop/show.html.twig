{% extends 'base.html.twig' %}

{% block title %}{{ shop.Label }}{% endblock %}

{% block body %}
    <h1 class='text-bold'><img src="{{ asset('uploads/images/logo/' ~ shop.logo) }}" class="logo" alt="{{ shop.Label }}" title="{{ shop.Label }}" /></h1>
    <!--{% if is_granted(['ROLE_USER','ROLE_ADD','ROLE_ADMIN']) %}
    {% for boutique in shop.fkCatShopId %}
        {% for prod in boutique.fkCatShopProdId %}
        <table class="table table-dark table-hover text-center">
            <thead>
                <tr>
                    <th>{{ boutique.Label }}</th>
                    <th>Prix</th>
                    <th>Stock</th>
                </tr>
            </thead>
            <tr>
                <td><a href="{{ path('products_show', {'id': prod.id}) }}" class="text-white">{{ prod.Label }}</a></td>
                <td>{{ prod.Price }} €</td>
                <td> *** </td>
            </tr>
        </table>
        {% endfor %}
    {% endfor %}
    {% elseif app.user == false %}
        {% for boutique in shop.fkCatShopId %}
            {% for prod in boutique.fkCatShopProdId %}
                <table class="table table-dark table-hover text-center">
                    <thead>
                        <tr>
                            <th>{{ boutique.Label }}</th>
                            <th>Prix</th>
                            <th>Stock</th>
                        </tr>
                    </thead>
                    <tr>
                        <td><a href="{{ path('products_show', {'id': prod.id}) }}" class="text-white">{{ prod.Label }}</a></td>
                        <td>{{ prod.Price }} €</td>
                        <td> *** </td>
                    </tr>
                </table>
            {% endfor %}
        {% endfor %}
        {% endif %}-->



            <!-- Page Content -->
            <div class="container">

                <div class="row">

                    <div class="col-lg-16">
                        <div class="row">
                            {% if is_granted(['ROLE_USER','ROLE_ADD','ROLE_ADMIN']) %}
                                {% for boutique in shop.fkCatShopId %}
                                    {% for prod in boutique.fkCatShopProdId %}
                                        <div class="container">
                                            <div class="card-deck mb-6 text-center">
                                                <div class="card mb-4 box-shadow">
                                                    <div class="card-header">
                                                        <h4 class="my-0 font-weight-normal"><a href="{{ path('products_show', {'id': prod.id}) }}" >{{ prod.Label }}</a></h4>
                                                    </div>
                                                    <div class="card-body">
                                                        <img class="rounded mx-auto d-block" src="{{ asset('uploads/images/produits/' ~ prod.ImageProducts) }}" alt="{{ prod.Label }}" title="{{ prod.Label }}" />
                                                        <ul class="list-unstyled mt-3 mb-4">
                                                            <li>Prix : {{ prod.Price }} €</li>
                                                            <li>Stock : *** </li>
                                                        </ul>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    {% endfor %}
                                {% endfor %}
                            {% else %}
                                {% for boutique in shop.fkCatShopId %}
                                    {% for prod in boutique.fkCatShopProdId %}
                                        <div class="container">
                                            <div class="card-deck mb-6 text-center">
                                                <div class="card mb-4 box-shadow">
                                                    <div class="card-header">
                                                        <h4 class="my-0 font-weight-normal"><a href="{{ path('products_show', {'id': prod.id}) }}" >{{ prod.Label }}</a></h4>
                                                    </div>
                                                    <div class="card-body">
                                                        <img class="rounded mx-auto d-block" src="{{ asset('uploads/images/produits/' ~ prod.ImageProducts) }}" alt="{{ prod.Label }}" title="{{ prod.Label }}" />
                                                        <ul class="list-unstyled mt-3 mb-4">
                                                            <li>Prix : {{ prod.Price }} €</li>
                                                            <li>Stock : *** </li>
                                                        </ul>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    {% endfor %}
                                {% endfor %}
                            {% endif %}
                        </div>
                        <!-- /.row -->

                    </div>
                    <!-- /.col-lg-12 -->

                </div>
                <!-- /.row -->

            </div>
            <!-- /.container -->


            {% if shop.fkUserId == app.user %}
                <div class="lien">
                    <a href="{{ path('user_index') }}">Retour</a>
                    <br />
                    <br />
                    <a href="{{ path('shop_edit', {'id': shop.id}) }}">Modifier</a>
                    <br />
                    <br />
                    <a href="{{ path('products_new', {'id': shop.id}) }}">Ajouter un produit à la boutique</a>
                </div>

                {{ include('shop/_delete_form.html.twig') }}
            {% else %}
                <div class="lien">
                    <a href="{{ path('shop_index') }}">Retour</a>
                </div>
            {% endif %}


            '<body style="background : url('{{ asset('uploads/images/boutique/' ~ shop.ShopImage) }}') no-repeat;background-size : cover;"></body>';




            {% endblock %}