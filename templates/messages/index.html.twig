{% extends 'base.html.twig' %}

{% block title %}Messages index{% endblock %}

{% block body %}
    <h1 class="text-center" style="margin-top: 20px;margin-bottom: 20px;">Messages index</h1>  
        
        <section class='content'>
            <div class='container'>
                <div class="col-md-3">
                    <div class="list-group">
                        {% for user in userId %}
                        <a class="list-group-item" href="{{ path('messages_show', {'id': user.id}) }}">{{ user.username }}</a>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </section>

    <!--<table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Content</th>
                <th>CreatedAt</th>
                <th>ReadAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {#{% for message in messages %}
    
            <tr>
                <td>{{ message.id }}</td>
                <td>{{ message.Content }}</td>
                <td>{{ message.CreatedAt ? message.CreatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ message.ReadAt ? message.ReadAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('messages_show', {'id': message.id}) }}">show</a>
                    <a href="{{ path('messages_edit', {'id': message.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}#}
        </tbody>
    </table>-->

    <!--<a href="{#{{ path('messages_new') }}#}">Create new</a>-->
{% endblock %}