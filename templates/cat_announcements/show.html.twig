{% extends 'base.html.twig' %}

{% block title %}CatAnnouncements{% endblock %}

{% block body %}
    <h1 class="text-center">Catégories des Annonces</h1>

    <table class="table table-dark table-hover text-center">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_announcement.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ cat_announcement.Label }}</td>
            </tr>
        </tbody>
    </table>

    <div class='lien'>
        <a class="btn btn-common log-btn" href="{{ path('cat_announcements_index') }}">Retour</a>
        <br /><br />
        <a class="btn btn-common log-btn"  href="{{ path('cat_announcements_edit', {'id': cat_announcement.id}) }}">Modifier</a>
        <br /> <br />
        {{ include('cat_announcements/_delete_form.html.twig') }}
    </div>
{% endblock %}