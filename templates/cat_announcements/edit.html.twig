{% extends 'base.html.twig' %}

{% block title %}Edit CatAnnouncements{% endblock %}

{% block body %}
    <h1>Edit CatAnnouncements</h1>

    {{ include('cat_announcements/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-common log-btn" href="{{ path('cat_announcements_index') }}">back to list</a>

    {{ include('cat_announcements/_delete_form.html.twig') }}
{% endblock %}