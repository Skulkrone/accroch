{% extends 'base.html.twig' %}

{% block title %}Editer une annonce{% endblock %}

{% block body %}
    <h1 class="text-bold text-white text-center" style="margin-top: 20px;">Modifier mon annonce</h1>

    {{ include('announcements/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="lien">
        <a class="btn btn-common log-btn" href="{{ path('announcements_index') }}">Retour</a>
        <br />
        <br />
        {{ include('announcements/_delete_form.html.twig') }}
    </div>
    
{% endblock %}