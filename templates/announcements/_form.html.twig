<section id="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-6 col-sm-offset-4 col-md-6 col-md-offset-3">
                <div class="page-login-form box">

                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn btn-common log-btn">{{ button_label|default('Publier') }}</button>
                    {{ form_end(form) }}

                </div>
            </div>
        </div>
    </div>
</section>
